# Enterprise stack connection
# These defaults match the baseline created by waltid-enterprise-quickstart:
#   docker compose up
#   cd cli && npm install
#   HOST_ALIAS_DOMAIN=<your-ngrok-domain> npx tsx walt.ts --init-system
#   HOST_ALIAS_DOMAIN=<your-ngrok-domain> npx tsx walt.ts --setup-all
# Then create mobile helper resources once:
#   ./e2e-local-enterprise.sh --prepare-only
PORT=7500
ADMIN_EMAIL=admin@walt.id
ADMIN_PASSWORD=admin123456
ORGANIZATION=waltid
TENANT=waltid-tenant01

# Ngrok tunnel domain for `ngrok http 7500` (required for the current mobile E2E setup).
# Use the host only, without https://.
HOST_ALIAS_DOMAIN=your-domain.ngrok-free.dev

# Optional overrides
# IDENTITY_DIR=/path/to/waltid-identity
# IOS_SIMULATOR_ID=<uuid>
# IOS_SIMULATOR_ARCHS=arm64
# SKIP_IOS_APP_SETUP=true

# Test data
# EUDI_CREDENTIAL_ID=eu.europa.ec.eudi.pid_vc_sd_jwt

# Enterprise resources
# ATTESTER_PATH=waltid.waltid-tenant01.client-attester
# Non-attested scripts default to issuer2-noattest.mdl-profile.
# Create it explicitly with ./e2e-local-enterprise.sh --prepare-only.
# Attested scripts default to the quickstart-created issuer2.mdl-profile.
# ISSUER_PROFILE=issuer2.mdl-profile
# The scripts default to verifier2-mobile because the quickstart-created verifier2
# uses a local base URL that is not usable by mobile cross-device flows.
# Create verifier2-mobile explicitly with ./e2e-local-enterprise.sh --prepare-only.
# VERIFIER=verifier2-mobile
