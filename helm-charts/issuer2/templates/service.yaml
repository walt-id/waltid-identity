apiVersion: v1
kind: Service
metadata:
  name: {{ include "issuer2.fullname" . }}-service
  labels:
    {{- include "issuer2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.type }}
  selector:
    {{ include "issuer2.selectorLabels" . | nindent 4 }}
  ports:
    {{- .Values.api.ports | toYaml | nindent 4 }}