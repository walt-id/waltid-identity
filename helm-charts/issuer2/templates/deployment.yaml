apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "issuer2.fullname" . }}
  labels:
    {{- include "issuer2.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      app: issuer2
      {{- include "issuer2.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: issuer2
      {{- include "issuer2.labels" . | nindent 8 }}
      annotations:
        deployment/id: _DEFAULT_DEPLOYMENT_    
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: issuer2
        image: {{ .Values.api.issuerApi2.image.repository }}:{{ .Values.api.issuerApi2.image.tag | default .Chart.AppVersion }}
        imagePullPolicy: {{ .Values.api.issuerApi2.image.imagePullPolicy }}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        ports:
          - name: http-api
            containerPort: 7002
        volumeMounts:
        {{- range $name, $content := .Values.configFiles }}
        - mountPath: /waltid-issuer-api2/config/{{ $name }}
          name: issuer2-web-config
          subPath: {{ $name }}
          readOnly: true
        {{- end }}
      volumes:
      - configMap:
          name: {{ include "issuer2.fullname" . }}-web-config
        name: issuer2-web-config