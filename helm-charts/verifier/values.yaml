api:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http-api
  replicas: 1
  type: ClusterIP
  verifierApi:
    image:
      repository: waltid/verifier-api
      tag: latest
      imagePullPolicy: Always

configFiles:
  _features.conf: |-
    enabledFeatures = [
        entra
    ]
  web.conf: |-
    webHost = "0.0.0.0"
    webPort = 3000
  verifier-service.conf: |-
    baseUrl = "https://{{ .Values.ingress.host }}"
  entra.conf: |-
    callbackUrl = "https://{{ .Values.ingress.host }}/entra/verification-callback/"

ingress:
  ingressClassName: "traefik"
  host: verifier.portal.waltid.cloud