apiVersion: v1
kind: Service
metadata:
  name: {{ include "wallet-api2.fullname" . }}-api
spec:
  type: {{ .Values.api.type }}
  selector:
    app: wallet-api2
    {{- include "wallet-api2.selectorLabels" . | nindent 4 }}
  ports:
    {{- .Values.api.ports | toYaml | nindent 4 }}
