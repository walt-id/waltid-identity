apiVersion: v1
kind: Service
metadata:
  name: {{ include "verifier2.fullname" . }}-service
  labels:
    {{- include "verifier2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.type }}
  selector:
    {{- include "verifier2.selectorLabels" . | nindent 4 }}
  ports:
    {{- .Values.api.ports | toYaml | nindent 4 }}
