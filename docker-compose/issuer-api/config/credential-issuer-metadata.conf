supportedCredentialTypes = {
    # ============================================
    # PHASE 1: SD-JWT PID (EUDI Wallet Compatible)
    # ============================================
    # EUDI wallet SDK only recognizes "dc+sd-jwt" format (not "vc+sd-jwt")
    "urn:eudi:pid:1" = {
        format = "dc+sd-jwt"
        vct = "urn:eudi:pid:1"
        scope = "urn:eudi:pid:1"
        cryptographic_binding_methods_supported = ["jwk"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = {
            jwt = { proof_signing_alg_values_supported = ["ES256", "ES384", "ES512"] }
        }
    }

    # ============================================
    # PHASE 2: mDoc PID
    # ============================================
    # EUDI wallet SDK uses JWT proofs for mDoc (CWT marked Unsupported in SDK)
    "eu.europa.ec.eudi.pid_mso_mdoc" = {
        format = mso_mdoc
        scope = "eu.europa.ec.eudi.pid_mso_mdoc"
        cryptographic_binding_methods_supported = ["jwk"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = {
            jwt = { proof_signing_alg_values_supported = ["ES256", "ES384", "ES512"] }
        }
        doctype = "eu.europa.ec.eudi.pid.1"
    }

    "eu.europa.ec.eudi.pid.1" = {
        format = mso_mdoc
        scope = "eu.europa.ec.eudi.pid.1"
        cryptographic_binding_methods_supported = ["jwk"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = {
            jwt = { proof_signing_alg_values_supported = ["ES256", "ES384", "ES512"] }
        }
        doctype = "eu.europa.ec.eudi.pid.1"
    }

    # ============================================
    # PHASE 3: mDL
    # ============================================
    # EUDI wallet SDK uses JWT proofs for mDoc (CWT marked Unsupported in SDK)
    "org.iso.18013.5.1.mDL" = {
        format = mso_mdoc
        scope = "org.iso.18013.5.1.mDL"
        cryptographic_binding_methods_supported = ["jwk"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = {
            jwt = { proof_signing_alg_values_supported = ["ES256", "ES384", "ES512"] }
        }
        doctype = "org.iso.18013.5.1.mDL"
    }
}
