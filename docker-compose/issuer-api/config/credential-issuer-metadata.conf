supportedCredentialTypes = {
    # JWT VC JSON credentials - matching VC Repo list
    "PID_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "PID"]
        }
    }

    "AlpsTourReservation_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "AlpsTourReservation"]
        }
    }

    "BankId_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "BankId"]
        }
    }

    "BoardingPass_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "BoardingPass"]
        }
    }

    "DataspaceParticipantCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "DataspaceParticipantCredential"]
        }
    }

    "EducationalID_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "EducationalID"]
        }
    }

    "eID_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "eID"]
        }
    }

    "EmailVerificationCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "EmailVerificationCredential"]
        }
    }

    "EnrollmentCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "EnrollmentCredential"]
        }
    }

    "ePassport_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "ePassport"]
        }
    }

    "GaiaXTermsAndConditions_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "GaiaXTermsAndConditions"]
        }
    }

    "HotelReservation_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "HotelReservation"]
        }
    }

    "IdentityCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "IdentityCredential"]
        }
    }

    "Iso18013DriversLicenseCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "Iso18013DriversLicenseCredential"]
        }
    }

    "KiwiAccessCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "KiwiAccessCredential"]
        }
    }

    "KycChecksCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "KycChecksCredential"]
        }
    }

    "KycCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "KycCredential"]
        }
    }

    "KycDataCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "KycDataCredential"]
        }
    }

    "LegalPerson_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "LegalPerson"]
        }
    }

    "LegalRegistrationNumberEORI_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "LegalRegistrationNumberEORI"]
        }
    }

    "LegalRegistrationNumberLeiCode_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "LegalRegistrationNumberLeiCode"]
        }
    }

    "LegalRegistrationNumberVatId_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "LegalRegistrationNumberVatId"]
        }
    }

    "MortgageEligibility_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "MortgageEligibility"]
        }
    }

    "NaturalPersonVerifiableID_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "NaturalPersonVerifiableID"]
        }
    }

    "OpenBadgeCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "OpenBadgeCredential"]
        }
    }

    "PassportCh_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "PassportCh"]
        }
    }

    "PhotoIDCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "PhotoIDCredential"]
        }
    }

    "PND91Credential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "PND91Credential"]
        }
    }

    "ProofOfAddress_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "ProofOfAddress"]
        }
    }

    "TaxCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "TaxCredential"]
        }
    }

    "TaxReceipt_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "TaxReceipt"]
        }
    }

    "UniversityDegree_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "UniversityDegree"]
        }
    }

    "VaccinationCertificate_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "VaccinationCertificate"]
        }
    }

    "VerifiableId_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "VerifiableAttestation", "VerifiableId"]
        }
    }

    "VerifiablePortableDocumentA1_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "VerifiablePortableDocumentA1"]
        }
    }

    "Visa_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "Visa"]
        }
    }

    "WalletHolderCredential_jwt_vc_json" = {
        format = "jwt_vc_json"
        cryptographic_binding_methods_supported = ["did"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        credential_definition = {
            type = ["VerifiableCredential", "WalletHolderCredential"]
        }
    }

    # mdoc format
    "org.iso.18013.5.1.mDL" = {
        format = mso_mdoc
        cryptographic_binding_methods_supported = ["cose_key"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { cwt = { proof_signing_alg_values_supported = ["ES256"] } }
        doctype = "org.iso.18013.5.1.mDL"
    }

    # EUDI PID mdoc - required for wallet activation
    "eu.europa.ec.eudi.pid.1" = {
        format = mso_mdoc
        cryptographic_binding_methods_supported = ["cose_key"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        doctype = "eu.europa.ec.eudi.pid.1"
    }

    # SD-JWT credentials
    # EUDI Reference Wallet expects exactly "urn:eudi:pid:1" for PID activation
    "urn:eudi:pid:1" = {
        format = "vc+sd-jwt"
        cryptographic_binding_methods_supported = ["jwk"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        vct = "urn:eudi:pid:1"
    }

    "identity_credential_vc+sd-jwt" = {
        format = "vc+sd-jwt",
        cryptographic_binding_methods_supported = ["jwk"]
        credential_signing_alg_values_supported = ["ES256"]
        proof_types_supported = { jwt = { proof_signing_alg_values_supported = ["ES256"] } }
        vct = "http://${SERVICE_HOST}:${ISSUER_API_PORT}/identity_credential"
    }
}
