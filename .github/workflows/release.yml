name: Release on manual push

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Release version'
        required: true

jobs:
  version:
    uses: walt-id/waltid-identity/.github/workflows/version.yml@591c1247ad96992dad70dfc73b374cc2fd1afd4d
    with:
      preferred: ${{ inputs.release_version }}
  gradle-build:
    uses: walt-id/waltid-identity/.github/workflows/build-gradle.yml@591c1247ad96992dad70dfc73b374cc2fd1afd4d
    secrets: inherit
    needs: version
    with:
      version: ${{ needs.version.outputs.release_version }}
      publish: true
  docker-build:
    uses: walt-id/waltid-identity/.github/workflows/build-docker.yml@591c1247ad96992dad70dfc73b374cc2fd1afd4d
    secrets: inherit
    needs: version
    with:
      version: ${{ needs.version.outputs.release_version }}
      tag: latest
      publish: true
  deploy:
    uses: walt-id/waltid-identity/.github/workflows/deployment.yml@591c1247ad96992dad70dfc73b374cc2fd1afd4d
    secrets: inherit
    needs: [ version, gradle-build, docker-build ]
    with:
      version: ${{ needs.version.outputs.release_version }}
      deployment: prod
      namespace: default
      portals: portals
      kubeconfig_secret: NL_KUBE_CONFIG
  tag:
    uses: walt-id/waltid-identity/.github/workflows/tag.yml@591c1247ad96992dad70dfc73b374cc2fd1afd4d
    needs: [ version, deploy ]
    with:
      version: ${{ needs.version.outputs.release_version }}
  release:
    uses: walt-id/waltid-identity/.github/workflows/changelog.yml@591c1247ad96992dad70dfc73b374cc2fd1afd4d
    secrets: inherit
    needs: [ tag ]
    with:
      tag: ${{ needs.tag.outputs.tag }}