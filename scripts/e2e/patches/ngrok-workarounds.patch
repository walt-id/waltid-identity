diff --git a/waltid-applications/waltid-wallet-demo-android/src/main/AndroidManifest.xml b/waltid-applications/waltid-wallet-demo-android/src/main/AndroidManifest.xml
index 2bacd6163..3fc910338 100644
--- a/waltid-applications/waltid-wallet-demo-android/src/main/AndroidManifest.xml
+++ b/waltid-applications/waltid-wallet-demo-android/src/main/AndroidManifest.xml
@@ -5,6 +5,7 @@
 
     <application
         android:allowBackup="true"
+        android:usesCleartextTraffic="true"
         android:label="@string/app_name"
         android:supportsRtl="true"
         android:theme="@android:style/Theme.Material.Light.NoActionBar">
diff --git a/waltid-libraries/protocols/waltid-openid4vc-wallet/src/commonMain/kotlin/id/walt/wallet2/handlers/WalletIssuanceHandler.kt b/waltid-libraries/protocols/waltid-openid4vc-wallet/src/commonMain/kotlin/id/walt/wallet2/handlers/WalletIssuanceHandler.kt
index 80306a3eb..96cf5778d 100644
--- a/waltid-libraries/protocols/waltid-openid4vc-wallet/src/commonMain/kotlin/id/walt/wallet2/handlers/WalletIssuanceHandler.kt
+++ b/waltid-libraries/protocols/waltid-openid4vc-wallet/src/commonMain/kotlin/id/walt/wallet2/handlers/WalletIssuanceHandler.kt
@@ -338,7 +338,7 @@ object WalletIssuanceHandler {
             }
             log.trace { "Posting to credential endpoint: ${credentialRequestJson.toString().take(200)}" }
 
-            val credentialResponse = httpClient.post(credentialEndpoint) {
+            val credentialResponse = postFollowingRedirects(httpClient, credentialEndpoint) {
                 header(HttpHeaders.Authorization, "Bearer ${tokenResponse.access_token}")
                 contentType(ContentType.Application.Json)
                 setBody(credentialRequestJson.toString())
@@ -462,7 +462,7 @@ object WalletIssuanceHandler {
                 }
             }
         }
-        val response = httpClient.post(request.credentialEndpoint.toString()) {
+        val response = postFollowingRedirects(httpClient, request.credentialEndpoint.toString()) {
             header(HttpHeaders.Authorization, "Bearer ${request.accessToken}")
             contentType(ContentType.Application.Json)
             setBody(credentialRequestJson.toString())
@@ -487,9 +487,25 @@ object WalletIssuanceHandler {
         else -> wallet.defaultKey()
     }
 
+    private suspend fun postFollowingRedirects(
+        httpClient: HttpClient,
+        url: String,
+        block: HttpRequestBuilder.() -> Unit
+    ): HttpResponse {
+        var response = httpClient.post(url, block)
+        if (response.status.value in listOf(301, 302, 303, 307, 308)) {
+            val location = response.headers[HttpHeaders.Location]
+            if (location != null) {
+                log.debug { "Following redirect to: $location" }
+                response = httpClient.post(location, block)
+            }
+        }
+        return response
+    }
+
     private suspend fun fetchCNonce(httpClient: HttpClient, nonceEndpoint: String): String? =
         runCatching {
-            val response = httpClient.post(nonceEndpoint) {
+            val response = postFollowingRedirects(httpClient, nonceEndpoint) {
                 contentType(ContentType.Application.Json)
             }
             if (response.status.isSuccess()) {
diff --git a/waltid-libraries/protocols/waltid-openid4vci-wallet/src/commonMain/kotlin/id/waltid/openid4vci/wallet/token/TokenRequestBuilder.kt b/waltid-libraries/protocols/waltid-openid4vci-wallet/src/commonMain/kotlin/id/waltid/openid4vci/wallet/token/TokenRequestBuilder.kt
index 38ea49002..b58870db4 100644
--- a/waltid-libraries/protocols/waltid-openid4vci-wallet/src/commonMain/kotlin/id/waltid/openid4vci/wallet/token/TokenRequestBuilder.kt
+++ b/waltid-libraries/protocols/waltid-openid4vci-wallet/src/commonMain/kotlin/id/waltid/openid4vci/wallet/token/TokenRequestBuilder.kt
@@ -119,8 +119,8 @@ class TokenRequestBuilder(
     ): TokenResponse {
         log.debug { "Sending token request to authorization server" }
         log.trace { "Request parameters count: ${parameters.names().size}" }
-        
-        val response: HttpResponse = try {
+
+        var response: HttpResponse = try {
             httpClient.post(tokenEndpoint) {
                 contentType(ContentType.Application.FormUrlEncoded)
                 setBody(parameters.formUrlEncode())
@@ -130,6 +130,17 @@ class TokenRequestBuilder(
             throw Exception("Failed to send token request", e)
         }
 
+        if (response.status.value in listOf(301, 302, 303, 307, 308)) {
+            val location = response.headers[HttpHeaders.Location]
+            if (location != null) {
+                log.debug { "Following redirect to: $location" }
+                response = httpClient.post(location) {
+                    contentType(ContentType.Application.FormUrlEncoded)
+                    setBody(parameters.formUrlEncode())
+                }
+            }
+        }
+
         if (!response.status.isSuccess()) {
             val errorBody = response.bodyAsText()
             log.error {
