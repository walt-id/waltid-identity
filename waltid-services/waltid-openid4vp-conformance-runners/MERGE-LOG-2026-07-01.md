# Merge Decision Log: main → feature/wal-896-conformance-test-consolidation

**Date**: 2026-07-01
**Merge commit**: `06fe4a023`

## Conflicts Resolved

### 1. WalletIssuanceHandler.kt

**Location**: `waltid-libraries/protocols/waltid-openid4vc-wallet/src/commonMain/kotlin/id/walt/wallet2/handlers/`

**Conflict sections**:
- Import statements (lines 5-12)
- `fetchCredential()` function (lines 624-655)

**Resolution**:
- **Imports**: Combined both sets
  - HEAD: `Key`, `JWKKey` (needed for DPoP proof generation)
  - Main: `CryptographicBindingMethod` (needed for binding method detection)
  - Removed: `DefaultClient`, `DefaultCredentialRequest` (unused in main)

- **fetchCredential()**: Kept HEAD version entirely
  - **Reason**: HEAD has DPoP support with nonce retry logic (RFC 9449 §5)
  - Main's version is simpler (Bearer token only) but lacks DPoP
  - DPoP is required for VCI wallet conformance tests (test plan 4 - private_key_jwt + DPoP)

### 2. TokenRequestBuilder.kt

**Location**: `waltid-libraries/protocols/waltid-openid4vci-wallet/src/commonMain/kotlin/id/waltid/openid4vci/wallet/token/`

**Conflict sections**:
- `executeTokenRequest()` method signature and body (lines 139-188)
- Post-method code: `executeTokenRequestWithNonce` vs `isSameOrigin` (lines 265-322)

**Resolution**: Kept BOTH overloads by renaming for clarity

| Method | Source | Purpose |
|--------|--------|---------|
| `executeTokenRequestWithDPoP()` | HEAD | Auth-code exchange with DPoP + private_key_jwt |
| `executeTokenRequest()` | Main | Pre-auth-code with attestation headers + redirect following |
| `executeTokenRequestWithNonce()` | HEAD | DPoP nonce retry (RFC 9449 §5) |
| `isSameOrigin()` | Main | Helper for redirect security checks |

**Reason**: Both flows are needed:
- `exchangeAuthorizationCode()` → uses DPoP version (conformance tests)
- `exchangePreAuthorizedCode()` → uses attestation version (pre-auth flows)

### 3. authentication-service.conf

**Location**: `waltid-services/waltid-issuer-api2/config/`

**Conflict**: `forwardIssuerStateToAuthorizationServer` value
- HEAD: `true`
- Main: `false` (with test user documentation)

**Resolution**: Used main's value (`false`)
- **Reason**: HEAD's `true` was for conformance testing; main's `false` is the intended default
- Kept main's helpful test user documentation comments

## Verification

- ✅ Build compiles successfully
- ✅ No conflict markers remain
- ✅ Both DPoP and attestation flows preserved
