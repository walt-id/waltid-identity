{
    sinks = {
        stdout = {
            renderWith = RENDER_ANSI
            sendTo = STDOUT
        }
    },
    logging = [
        {
            levelRanges = [
                {
                    fromMinLevel = TRACE
                    toSinks = [stdout]
                }
            ]
        }
    ]
    minDirectLogLevel = TRACE
}
