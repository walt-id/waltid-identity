wallet-service {
    # Public-facing base URL of this wallet service.
    # Used in wallet creation responses and OpenID4VCI redirect URIs.
    publicBaseUrl = "http://localhost:4000"
    publicBaseUrl = ${?WALLET_PUBLIC_BASE_URL}
}

# Optional: enable user authentication (disabled by default)
# To enable: set auth.enabled = true and configure auth.jwtSecret
# auth {
#     jwtSecret = "change-me-in-production-min-32-chars"
#     jwtSecret = ${?WALLET_AUTH_JWT_SECRET}
#     tokenExpirySeconds = 86400
# }
