# Android SDK location (required for Android builds)
# Example: sdk.dir=/Users/username/Library/Android/sdk
sdk.dir=/path/to/android-sdk

# Optional CocoaPods binary path override (macOS only).
# Leave unset when `pod` is already available on PATH.
# Example: Apple Silicon Macs often use /opt/homebrew/bin/pod; Intel Macs often use /usr/local/bin/pod.
# kotlin.apple.cocoapods.bin=/path/to/pod

# Optional local platform build flags.
# These are ignored by Git when set in local.properties.
# Command-line -P flags take precedence over local.properties.
# local.properties takes precedence over the tracked defaults in gradle.properties.
enableAndroidBuild=true
enableIosBuild=true
