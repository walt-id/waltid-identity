package id.walt.did.dids.registrar.local.algo

import id.walt.did.dids.document.DidDocument


actual fun saveDidDocumentToAlgorand(didDocument: DidDocument) {
    console.log("Saving DID Document to Algorand (JS): didDocument")
    // Use a JavaScript library or REST API call to save the document
}
