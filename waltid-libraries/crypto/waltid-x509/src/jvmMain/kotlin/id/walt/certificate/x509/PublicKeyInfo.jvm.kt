package id.walt.certificate.x509

actual suspend fun PublicKeyInfo.Companion.ofKey(key: id.walt.crypto2.keys.Key): PublicKeyInfo {
    TODO("Not yet implemented")
}

actual suspend fun PublicKeyInfo.Companion.ofKey(key: id.walt.crypto.keys.Key): PublicKeyInfo {
    TODO("Not yet implemented")
}