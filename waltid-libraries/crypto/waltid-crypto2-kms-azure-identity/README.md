# waltid-crypto2-kms-azure-identity

Optional JVM Azure Identity bridge for the crypto2 Azure Key Vault REST provider. It supplies access tokens from `DefaultAzureCredential` or another injected `TokenCredential` without duplicating Key Vault cryptographic operations.
